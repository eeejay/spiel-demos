name: eSpeak Speech Provider Flatpak

on: workflow_dispatch

jobs:
  flatpak:
    name: Flatpak
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/andyholmes/flatter/freedesktop:23.08
      options: --privileged
    permissions:
      contents: write

    strategy:
      max-parallel: 1
      matrix:
        arch: [x86_64]
      fail-fast: false

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup GPG
        id: gpg
        uses: crazy-max/ghaction-import-gpg@v6
        with:
          gpg_private_key: ${{ secrets.GPG_PRIVATE_KEY }}

      - name: Install llvm17 extension
        run: flatpak install --noninteractive org.freedesktop.Sdk.Extension.llvm17

      - name: Install rust extension
        run: flatpak install --noninteractive org.freedesktop.Sdk.Extension.rust-stable

      - name: Build
        id: build
        uses: andyholmes/flatter@main
        with:
          files: |
            providers/espeak/build-aux/org.espeak.Speech.Provider.json
          arch: ${{ matrix.arch }}
          gpg-sign: ${{ steps.gpg.outputs.fingerprint }}
          upload-bundles: true

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@releases/v4
        with:
          folder: ${{ steps.build.outputs.repository }}
          target-folder: repo
          single-commit: true